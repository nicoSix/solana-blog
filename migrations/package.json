{
  "name": "migrations",
  "version": "1.0.0",
  "description": "Used to deploy program",
  "main": "build.js",
  "scripts": {
    "anchor:build": "npm install && node build.js",
    "anchor:deploy": "npm install && node deploy.js",
    "anchor:upgrade": "npm install && node upgrade.js"
  },
  "author": "Nicolas Six",
  "license": "ISC",
  "dependencies": {
    "cross-spawn": "^7.0.3",
    "dotenv": "^16.0.3",
    "path": "^0.12.7"
  }
}
